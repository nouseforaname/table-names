steps:
- task: inject-jars-into-container
  config:
    platform: linux
    inputs:
    - name: dependencies-conn-provider
    - name: ependencies-driver
    outputs:
    - name: mnt
    image_resource:
      type: registry-image
      source: { repository: ellerbrock/alpine-bash-git }
    run:
      path: bash
      args:
        - -c
        - |
          mkdir -p mnt/vol
          cp dependencies/* mnt/vol/
