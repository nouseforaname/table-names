parallel_tasks:
  limit: ((concurrent_limit))
  steps:
  - task: write-tables
    config:
      platform: linux
      image_resource:
        type: registry-image
        source: { repository: ellerbrock/alpine-bash-git }
      run:
        path: bash
        args:
          - -c
          - |
            echo done
